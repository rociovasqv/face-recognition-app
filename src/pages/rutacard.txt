Esta variable hay que agregar para traer el componente card redirigido por datos incorrectos

const Login = () => {
  const location = useLocation();
  const errorMessage = location.state?.errorMessage;

  return (
    <div>
      {errorMessage && <p>{errorMessage}</p>}
      {/* Aquí va tu formulario de login */}
    </div>
  );
};

CAMBIOS QUE SE REALIZARON EN LA TARJTA


Añadí react-router-dom: Importé y usé react-router-dom para manejar las rutas y la redirección.
Manejo de redirección y mensajes de error en Card: Implementé la lógica en Card para manejar el estado del mensaje de error y la redirección al login.
Login: En el Login que el mensaje de error es redirigido con un estado, hay que agregar(el uselocation y error mesage).
Configuración de rutas en App: Configuré las rutas para manejar la navegación entre la vista principal y el login.
TODO COMENTADO PARA NO ROMPER NADA.